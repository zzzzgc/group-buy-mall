// 配置

// 访问站点
const host = 'http://localhost:8100'
const timeout = 10000
// 微信appid
const appid = ''
// 微信key?不放这
const appKey = ''

const fileUpdateUrl = 'http://127.0.0.1:8100/file'

const config = {
  host,
  timeout,
  appid,
  appKey,
  fileUpdateUrl
}
export default config
